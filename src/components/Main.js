import { ArticlePanel } from './ArticlePanel';
import { SideBar } from './SideBar';

export const Main = () => {
    return (
        <main>
            <ArticlePanel/>
            <SideBar/>
        </main>
    );
};